const About = (props) => {
    return (
        <div className="About">
            This is a React Router App to find data on stocks. Navigate to the Stocks page and select and individual stock to view data.
        </div>
    )
}

export default About